# TEDxBerkeley Statics

# Installation

First, install npm.

    npm install
    
Next, generate the `global.json` file (only needs to be done once)

    gulp global_json
    
Next, start gulp.

    gulp